import styled from 'styled-components';
import * as ss from 'styled-system';

export const Image = styled.img`
  ${ss.color}
  ${ss.space}
  ${ss.width}
  ${ss.height}
  ${ss.flex}
  ${ss.display}
  ${ss.justifyContent}
  ${ss.opacity}
  ${ss.position}
`;
